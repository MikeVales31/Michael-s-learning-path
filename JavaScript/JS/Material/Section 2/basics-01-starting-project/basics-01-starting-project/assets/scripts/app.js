/*
Variables 
Data storage  (let)

Constants
you cannot change the value (const)


You are allowed to skip semicolon at the end of the line
*/

let currentResult = 0;

currentResult = (currentResult + 10) * 3 / 2 - 1;

let calcuculationDescription = " (" + currentResult +  " + 10) * 3 / 2 - 1"

outputResult(currentResult, calcuculationDescription);