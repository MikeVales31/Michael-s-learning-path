/**
 * Operators
 */


//Aritmetic operators +, -, *, /, %

let operation = 30 % 20;
console.log(operation);

/**Assignment operators
 * =, +=, -=, *=, /=, **=, %= 
 */

/**
 * Operator of concatenation
 */

//+
let n = "beto"
let lastName = "quiroga"
let completeName = n + " " + lastName;

console.log(completeName)

//Template strings 

let nickname = `I am ${15+10} years old and my brother have ${17+10}`
console.log(nickname)

 //Comparison
 // == it can be from different types
 let a = 50
 let b = "50"

 let result1 = a == b 
 console.log(result1)

 // === strictly equal (value and type of data)

result = result1 === 10
 console.log(result)

 // != different

 result2 =result1 != result
 console.log(result2)

/**
 * Logic operators
 * and (&&), or (||)
 */

/**
 * Unary operator
 *  ++, --
 */

/**
 * ternary operator 
 * (condition)? result:result if not
 */

/**
 * Type coercion
 */

