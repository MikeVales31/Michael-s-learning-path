/**
 * When your object has a prototype and that prototype has another we called 
 * multilevel inheritance.
 */
let  myArray = [];

console.log(myArray);

/**
 * Objects created by a given constructor will have the same prototype
 */