/**
 * Prototype is a parent of another object or object base
 */

let x = {};
x.toString();

let y = {};

console.log(x);

/**
 * When we access to a property or a method on a object,
 * first access to the object, if it can´t  find it,
 * it access to the prototype 
 */