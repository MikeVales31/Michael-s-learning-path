/*
The rollup operator

The rollup operator only applies to columns that aggregate values, it calculates the summary
of each group as well as the entier result set.

Order:
SELECT
FROM
JOIN 
GROUP BY    WITH ROLLUP
*/