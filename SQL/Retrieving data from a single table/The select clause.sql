/*
Select clause

(SELECT *) will return all the columns.
(SELECT "specify the name of the columns") show only the specified.
(SELECT "column" +,-,*,/,%) you can do arithmetic expressions.
(SELECT DISTINCT "name of the column") it will show only the different values.

(AS) to give a name to the column. 



*/