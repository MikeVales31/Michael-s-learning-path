/*
The in operator

So we use the IN operator when you want to compare an attribute to the list values.
Instead of combining multiple or operators we use the in operator.

WHERE "name of the column" IN ('','','')
*/