/*
Numeric functions

ROUND(#) it will round the hole number, if you add a comma it will only round the digitis indicated
TRUNCATE(#,#) it only take the the decimals indicated
CEILING(#) return the smallest int grater than if has decimals or equal.
FLOOR(#) return the smallest int less than if has decimals or equal.
ABS() to calculate the absolute value of a number.
RAND() generate a random number between 0 and 1.

IF you want to see the complete list of functions only search "mysql numeric functions" in the browser.

*/