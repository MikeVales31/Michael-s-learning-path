/*
Updating multiple rows

The syntax is the same than in updating a single row, but the condition should be more general

UPDATE
SET
WHERE

You will get a problem only in MySQL workbench because of safe updates option, you need to uncheck.

*/  