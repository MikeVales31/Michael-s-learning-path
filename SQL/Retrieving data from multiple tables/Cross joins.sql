/*
Cross joins

We use cross joins to combine or join every record from the first table with every record
in the second table.

CROSS JOIN explicit syntax

We takea multiple tables in "FROM" to do a implicit cross join 
*/