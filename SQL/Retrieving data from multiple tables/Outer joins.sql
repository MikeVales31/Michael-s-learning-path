/*
Outer joins

 It is used to show all the data, not only the one that match in the condition.

 types:
 LEFT JOIN (all the records from the left table are returned "FROM")
 RIGHT JOIN (all the records from the right table are returned "JOIN") 

 Note: it is optional to use the OUTER keyword "LEFT OUTER JOIN" and "RIGHT OUTER JOIN" they are the same.



*/