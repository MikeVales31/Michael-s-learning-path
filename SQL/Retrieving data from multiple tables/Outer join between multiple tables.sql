/*
Outer join between multiple tables

you can add multiple outer joins like we do multiple joins, it is best practice to do only 
LEFT JOINS. It is important to follow the order:

SELECT 
FROM 
LEFT JOIN
LEFT JOIN
LEFT JOIN....
*/