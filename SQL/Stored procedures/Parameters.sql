/*
Parameters

We use parameters to pass a value to a stored procedure or to send values
to the calling program.

When you create a stored procedure in the parenthesis you add the parameters.

Note: all the parameters are mandatory.

Example:

CREATE PROCEDURE get_clients_by_state
(
state CHAR(2) 
)

*/