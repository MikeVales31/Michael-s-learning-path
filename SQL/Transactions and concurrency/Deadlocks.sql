/*
Deadlocks

A deadlock happens when different transactions cannot complete,
because each transaction holds a lock that the other needs, so both transactions 
need each other and never release their lock.


*/