/*
Static members

static are also called class members this are the fields and methods 
that belongs to a class, we use it when a value is independent of objects.
The static methods only can access to other static methods.
*/

package Classes;

public class StaticMembers {
    
}
