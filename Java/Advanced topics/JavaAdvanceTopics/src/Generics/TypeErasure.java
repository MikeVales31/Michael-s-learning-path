/*
Type erasure 

Is when java compiler erases the type parameters and replaces with the constraints, and if 
there are no constraints it will replace with the object class.

Note: you can view it on the byte code.
Note: when you have multiple constraints it will change the type to the 1°.
 
*/

package Generics;

public class TypeErasure {
    
}
