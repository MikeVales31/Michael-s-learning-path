/*
The comparable interface

We use it to compare two objects.

Note: you have to implement in the class using the keyword "implements"

Example:

    public class Generic implements Comparable<"generic type">{
        @Override
        public int compareTo("generic type"){
            return ....
        }
    }

Note: You should always specify the generic type argument.
*/

package Generics;

public class TheComparableInterface {
    
}
