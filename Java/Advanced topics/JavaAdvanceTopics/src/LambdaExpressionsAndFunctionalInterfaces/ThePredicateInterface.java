/*
The predicate interface

Represents an operation that takes an object and checks to see if the object 
satisfies some criteria (filter data).

Note: The next are some of the specializations.

1) BiPredicate (2 arguments, returns a boolean).
2) "Primitive"Predicate ("primitive", returns a boolean).

Example:

        Predicate <"Type"> name = "parameter" -> "comparison"
        name.test("type");
*/

package LambdaExpressionsAndFunctionalInterfaces;

public class ThePredicateInterface {
    
}
