/*
Variable capture

Lambda expressions

- "this" is a reference to the enclosing object.
- don't have instance fields (state) just cam access to the statice and fields of the class,

Anonymous inner classes 

- "this" is a reference to the current instance of the anonymous inner class.
- can have state so, they can have fields.

*/

package LambdaExpressionsAndFunctionalInterfaces;

public class VariableCapture {
    
}
