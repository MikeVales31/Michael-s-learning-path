/*
The unary operator

Represents a function, takes an argument of type "T" and returns a value of type "T".

Example:

        UnaryOperator<Integer> square = n -> n * n;
        square.apply(#);

Note: you can combine multiple with the andThen method.
*/

package LambdaExpressionsAndFunctionalInterfaces;

public class heUnaryOperator {
    
}
