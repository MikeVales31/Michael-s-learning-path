/*
Atomic objects


Use atomic classes.

You need to change the field you are going to pass to type Atomic "type" and set it 
to a new Atomic "type"().

Note: are faster and easier to use for counters.
*/

package ConcurrencyAndMultiThreading;

public class AtomicObjects {
    
}
