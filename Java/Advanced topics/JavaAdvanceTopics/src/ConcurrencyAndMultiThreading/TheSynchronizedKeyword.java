/*
The synchronized keyword

Note: it is for the same propose that locks, but it is better to not use it.

Note: the synchronizeed block, inside a method.

synchronized( monitor object){

}

Note: the monitor object is an object created in the fields.

Note: you can create a method using the synchronized keyword but this will synchronized
      on the current object.

Example:

        Access modifier + "synchronized" + return type + name (){ }

*/


package ConcurrencyAndMultiThreading;

public class TheSynchronizedKeyword {
    
}
