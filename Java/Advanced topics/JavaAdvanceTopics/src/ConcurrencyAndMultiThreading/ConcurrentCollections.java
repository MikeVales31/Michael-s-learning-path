/*
Concurrent collections


These classes use th partitioning technique (faster than synchronized collections)

Note: you can see it on java.util.concurrent package

Example:

        Map<String, Integer> map ? new ConcurrentHashMap<>();

Note: if you use somethinglike map everything else is the same.

*/


package ConcurrencyAndMultiThreading;

public class ConcurrentCollections {
    
}
