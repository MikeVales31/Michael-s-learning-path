/*
Locks 

Note: you use it, when you share the same object across many threads.

Note: follow the next steps.

1) In the class create a field of type Lock and set it to a new ReentrantLock().
2) In a method call the lock and use the lock() method.
3) At the end of the method use the unlock method.
*/

package ConcurrencyAndMultiThreading;

public class Locks {
    
}
