/*
Interrupting a thread

Note: steps.

1) Create a threa.
2) Use the interrupt() method.                                Note: it is a request

3) In your code check for interrupt request.
    if(Thread.currentThread().isInterrupted())
        
        break / return
*/


package ConcurrencyAndMultiThreading;

public class InterruptingAThread {
    
}
