/*
The volatile keyword

Solves visibility problem but not the race condition.

Note: you create a field (flag) as volatile.

Example:

    Access modifier + "volatile" + return type + name.


Caching has small amount of memory available locally.
*/

package ConcurrencyAndMultiThreading;

public class TheVolatileKeyword {
    
}
