/*
Slicing streams

Note: use the next methods.

1) limit(#)
2) skip(#)
3) takeWhile(predicate)                 Note: until the condition gets complete it will take
4) dropWhile(predicate)                 Note: it will drop until the condition completes

*/

package Streams;

public class SlicingStreams {
    
}
