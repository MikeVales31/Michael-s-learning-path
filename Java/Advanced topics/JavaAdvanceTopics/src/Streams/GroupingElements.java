/*
Grouping elements

Note: steps.

1) Create a enum.                                            Note: it is equal that creating a class
2) In the class you use declare it as a field.
3) Create a constructor. 
4) Create a getter of the enum.
5) Create a stream.
6) collect method ("Collectors class".groupBy("getter of enum")) 
*/

package Streams;

public class GroupingElements {
    
}
