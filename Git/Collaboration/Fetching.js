/*
Fetching

repository is not connected with the remote repository, to download the changes we use fetch.

To use the repository of the cloud into your local
(git fetch ) to download the new commit of the
(git merge)

You can create or edit 
*/