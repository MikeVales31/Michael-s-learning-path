/*
Squash merging 

Instead of making more branches it adds to a new commit the changes.
(git merge --squash "name of the branch")

Create a commit

It´s important to delete the branch in order to not get confused,
(git branch -D "name of the branch")
*/