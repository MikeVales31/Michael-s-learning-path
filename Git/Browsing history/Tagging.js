/*
Tagging

(git tag "version" "ID")light weigth tags to the commited of the ID
(git tag -a "version" -m) allocated tags can associate a message with a tag
(git tag -n) shows tags and messages
(git tag) shows tags
(git tag -d "tag name/version") delete a tag
*/