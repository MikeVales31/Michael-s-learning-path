/*
Dropping commits 

(git rebase -i "commit ID")

change the command form pick to drop or only delete the line.
If you have a conflict use the mergetool (git mergetool).
Then continue with the rebase (git rebase --continue).

*/