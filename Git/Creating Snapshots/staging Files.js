/*
Use (echo "content" > "place to store".txt) => echo hello > file1.txt
the previous code save hello in file1.txt

The git status. 

To add to the staging area we use the git add command,
if we use git add *.txt it adds all the files with txt extension,
If we use git add . it adds the whole directory.

To modify a file 
echo world >> file1.txt
and we need to add again (git add file1.txt)

*/