/*
1) Create a directory for our proyect (mkdir  "name")
2) Enter to the directory (cd "name")
3) Initialize a new repository (git init)
4) Check that we do the repository (ls) or if it is hidden (ls -a)
5) Open in Windows 10 is (start .git) other (open .git)
6) Remove git repository (rm -rf .git)
*/